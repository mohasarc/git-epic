// Single flip point for the closing-stage domain cutover.
export const CREDIT_LINE = '✦ forge yours at git-epic.dev';

export function epicOfLine(handle: string): string {
  return `The Epic of ${handle}`;
}
